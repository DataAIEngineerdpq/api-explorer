services:
  db:
    image: pgvector/pgvector:pg16
    container_name: api-explorer-db
    environment:
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: api_explorer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:                